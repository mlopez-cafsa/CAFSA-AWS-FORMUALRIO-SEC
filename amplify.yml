version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Sitio estático CAFSA - sin proceso de compilación"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
