version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site - no build step"
    build:
      commands:
        - echo "Ready to deploy"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
